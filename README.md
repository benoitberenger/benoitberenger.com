# benoitberenger.com

Personal website of Benoît Berenger — IT consultant.

Static site: plain HTML, CSS and JavaScript, no build step.

- `index.html` — single-page layout
- `style.css` — design tokens and styles
- `script.js` — EN/FR translations and language toggle (`?lang=fr` forces French)

Deploy by serving the repo root as static files.
